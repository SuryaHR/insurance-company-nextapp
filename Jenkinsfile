// Powered by Infostretch 

timestamps {

node () {

	stage ('Next Insurance Company Portal - Checkout') {
 	 checkout([$class: 'GitSCM', branches: [[name: '*/devops']], doGenerateSubmoduleConfigurations: false, extensions: [], submoduleCfg: [], userRemoteConfigs: [[credentialsId: 'credential-git', url: 'https://github.com/SuryaHR/insurance-company-nextapp.git']]]) 
	}
	stage ('Next Insurance Company Portal - Build') {
 	
// Unable to convert a build step referring to "com.mxstrive.jenkins.plugin.contentreplace.ContentReplaceBuilder". Please verify and convert manually if required. 
	}
}
}